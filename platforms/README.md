# Platform Organization

- `platforms/windows`: Windows release branches and build artifacts.
- `platforms/android`: Android-specific artifacts and release notes.
- `platforms/ios`: iOS-specific artifacts and release notes.

Current release follows: `beta-<number>/build-<number>`.
