# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow helps you trigger a SonarCloud analysis of your code and populates
# GitHub Code Scanning alerts with the vulnerabilities found.
# Free for open source project.

# 1. Login to SonarCloud.io using your GitHub account

# 2. Import your project on SonarCloud
#     * Add your GitHub organization first, then add your repository as a new project.
#     * Please note that many languages are eligible for automatic analysis,
#       which means that the analysis will start automatically without the need to set up GitHub Actions.
#     * This behavior can be changed in Administration > Analysis Method.
#
# 3. Follow the SonarCloud in-product tutorial
#     * a. Copy/paste the Project Key and the Organization Key into the args parameter below
#          (You'll find this information in SonarCloud. Click on "Information" at the bottom left)
#
#     * b. Generate a new token and add it to your Github repository's secrets using the name SONAR_TOKEN
#          (On SonarCloud, click on your avatar on top-right > My account > Security
#           or go directly to https://sonarcloud.io/account/security/)

# Feel free to take a look at our documentation (https://docs.sonarcloud.io/getting-started/github/)
# or reach out to our community forum if you need some help (https://community.sonarsource.com/c/help/sc/9)

name: SonarCloud analysis

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  pull-requests: read # allows SonarCloud to decorate PRs with analysis results

jobs:
  Analysis:
    runs-on: ubuntu-latest
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_PROJECT_KEY: ${{ vars.SONAR_PROJECT_KEY }}
      SONAR_ORGANIZATION: ${{ vars.SONAR_ORGANIZATION }}

    steps:
      - name: Summarize Sonar configuration presence (sanitized)
        # Write presence/absence of the relevant entries to the GitHub job summary; secrets are never printed
        shell: bash
        run: |
          echo "## SonarCloud configuration presence" >> "$GITHUB_STEP_SUMMARY"

          if [ -n "${{ vars.SONAR_PROJECT_KEY }}" ]; then
            echo "- SONAR_PROJECT_KEY: present" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "- SONAR_PROJECT_KEY: MISSING" >> "$GITHUB_STEP_SUMMARY"
          fi

          if [ -n "${{ vars.SONAR_ORGANIZATION }}" ]; then
            echo "- SONAR_ORGANIZATION: present" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "- SONAR_ORGANIZATION: MISSING" >> "$GITHUB_STEP_SUMMARY"
          fi

          if [ -n "${{ secrets.SONAR_TOKEN }}" ]; then
            echo "- SONAR_TOKEN: present (redacted)" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "- SONAR_TOKEN: MISSING" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: SonarCloud configuration notice
        # show the notice when the secret or required variables are missing (explicit existence checks)
        if: ${{ !secrets.SONAR_TOKEN || !vars.SONAR_PROJECT_KEY || !vars.SONAR_ORGANIZATION }}
        shell: bash
        run: |
          echo "::notice title=SonarCloud skipped::Configure the SONAR_TOKEN secret and SONAR_PROJECT_KEY and SONAR_ORGANIZATION repository variables to enable analysis."
          echo "## SonarCloud analysis skipped" >> "$GITHUB_STEP_SUMMARY"
          echo "The required SONAR_TOKEN secret and/or project configuration variables are not available." >> "$GITHUB_STEP_SUMMARY"

      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        if: ${{ secrets.SONAR_TOKEN && vars.SONAR_PROJECT_KEY && vars.SONAR_ORGANIZATION }}

      - name: Analyze with SonarCloud
        if: ${{ secrets.SONAR_TOKEN && vars.SONAR_PROJECT_KEY && vars.SONAR_ORGANIZATION }}

        # You can pin the exact commit or the version.
        # uses: SonarSource/sonarcloud-github-action@4006f663ecaf1f8093e8e4abb9227f6041f52216 # v2.2.0
        uses: SonarSource/sonarcloud-github-action@4006f663ecaf1f8093e8e4abb9227f6041f52216
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}   # Generate a token on Sonarcloud.io, add it to the secrets of this repo with the name SONAR_TOKEN (Settings > Secrets > Actions > add new repository secret)
        with:
          # Additional arguments for the SonarScanner CLI
          args:
            # Unique keys of your project and organization. You can find them in SonarCloud > Information (bottom-left menu)
            # mandatory
            -Dsonar.projectKey=${{ vars.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ vars.SONAR_ORGANIZATION }}
            # Comma-separated paths to directories containing main source files.
            #-Dsonar.sources= # optional, default is project base directory
            # Comma-separated paths to directories containing test source files.
            #-Dsonar.tests= # optional. For more info about Code Coverage, please refer to https://docs.sonarcloud.io/enriching/test-coverage/overview/
            # Adds more detail to both client and server-side analysis logs, activating DEBUG mode for the scanner, and adding client-side environment variables and system properties to the server[...]
            #-Dsonar.verbose= # optional, default is false
          # When you need the analysis to take place in a directory other than the one from which it was launched, default is .
          projectBaseDir: .
